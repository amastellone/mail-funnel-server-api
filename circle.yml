machine:
  ruby:
deployment:
  staging:
    branch: master
    heroku:
      appname: mail-funnel-server
# test:
#   post:
#     - bundle exec rake test=
test:
  override:
    - echo "Skipping Tests (For now, until somebody can clean them up)"