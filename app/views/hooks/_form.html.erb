<%= form_for(hook) do |f| %>
  <% if hook.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hook.errors.count, "error") %> prohibited this hook from being saved:</h2>

      <ul>
      <% hook.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_area :name %>
  </div>

  <div class="field">
    <%= f.label :identifier %>
    <%= f.text_area :identifier %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
