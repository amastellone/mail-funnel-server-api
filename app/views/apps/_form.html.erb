<%= form_for(app) do |f| %>
  <% if app.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(app.errors.count, "error") %> prohibited this app from being saved:</h2>

      <ul>
      <% app.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_local_id %>
    <%= f.number_field :user_local_id %>
  </div>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :api_key %>
    <%= f.text_field :api_key %>
  </div>

  <div class="field">
    <%= f.label :api_secret %>
    <%= f.text_area :api_secret %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
