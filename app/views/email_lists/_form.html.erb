<%= form_for(email_list) do |f| %>
  <% if email_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(email_list.errors.count, "error") %> prohibited this email_list from being saved:</h2>

      <ul>
      <% email_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :app_uuid_id %>
    <%= f.text_field :app_uuid_id %>
  </div>

  <div class="field">
    <%= f.label :user_local_id %>
    <%= f.number_field :user_local_id %>
  </div>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
